class DataTypes
{
    // Declaración explícita
    // int num = 5;
    // string message = "Hello World";
    // decimal price = 19.99m;
    //WriteLine($" Cantidad: {num}, Saludo: {message}, Precio: {price:C}");

    // Declaración con var
    // var gretting = "Hello";
    // decimal porcentage = 20.00m;
    //WriteLine($" Saludo: {gretting}, Porcentaje: {porcentage}%");
}